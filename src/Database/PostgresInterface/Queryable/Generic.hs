module Database.PostgresInterface.Queryable.Generic
  ( -- Generic deriving support for Queryable
    -- Usage: deriving (Generic, Queryable) via GenericQueryable MyType
  ) where

-- Generic deriving implementation will live here (Phase 8)
